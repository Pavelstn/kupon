<h1>Listing promotions</h1>

<table>
  <tr>
    <th>Region</th>
    <th>Partner</th>
    <th>Category</th>
    <th>User</th>
    <th>Is active</th>
    <th>Is delete</th>
    <th>Who delete</th>
    <th>On top</th>
    <th>Start action</th>
    <th>Stop action</th>
    <th>Title</th>
    <th>Short description</th>
    <th>Description</th>
    <th>Terms</th>
    <th>Costs</th>
    <th>Volume</th>
    <th>Left</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @promotions.each do |promotion| %>
  <tr>
    <td><%= promotion.region_id %></td>
    <td><%= promotion.partner_id %></td>
    <td><%= promotion.category_id %></td>
    <td><%= promotion.user_id %></td>
    <td><%= promotion.is_active %></td>
    <td><%= promotion.is_delete %></td>
    <td><%= promotion.who_delete %></td>
    <td><%= promotion.on_top %></td>
    <td><%= promotion.start_action %></td>
    <td><%= promotion.stop_action %></td>
    <td><%= promotion.title %></td>
    <td><%= promotion.short_description %></td>
    <td><%= promotion.description %></td>
    <td><%= promotion.terms %></td>
    <td><%= promotion.costs %></td>
    <td><%= promotion.volume %></td>
    <td><%= promotion.left %></td>
    <td><%= link_to 'Show', promotion %></td>
    <td><%= link_to 'Edit', edit_promotion_path(promotion) %></td>
    <td><%= link_to 'Destroy', promotion, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Promotion', new_promotion_path %>
